Read `RESEARCH.md` first. It is the local, rolling research-context file for this repo and should capture the user's goals, preferences, decisions, progress, verified findings, and next steps. `RESEARCH.md` is intentionally gitignored; update it as work progresses so future threads can recover context quickly. If `THREAD_PROMPT.md` exists, use it as the current handoff prompt for follow-on work. These handoff-reading instructions belong here in `AGENTS.md`, not in `MEMORY_BLOAT_README.md`.

Also maintain `MEMORY_BLOAT_README.md` as the persistent human resume doc for this repo. Whenever you install dependencies, fetch source trees, change build prerequisites, or otherwise change local machine/repo state that matters for resuming from a fresh SSH session, update it as a short numbered list of succinct setup/resume steps with exact commands. Do not use `sed` dump commands there, and do not turn it into an agent-oriented handoff file. If local source trees such as `external/linux/ubuntu-6.8.0-101.101/` may be missing on another machine or checkout, include the exact recreate steps in `MEMORY_BLOAT_README.md` instead of relying on `.gitignore` behavior.

As I talk to you, I want you to fill this file with important relevant details about me. Specifically, my research goals, specific intents, progress, and what I need to do next / whats remaining.

After you do a certain major task, you should create a .md file describing what you did precisely and why it works and the approaches you considered and took and why and pros and cons
