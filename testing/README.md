# testing/

This directory is primarily for agent-generated tests to verify code changes. These are not part of the project's main test suite (which uses `make lint`). They exist so that AI coding agents can write and run quick validation tests against new or modified code without polluting the core codebase.
